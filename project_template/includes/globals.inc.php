<?php

// Define your plugin dir constant if you want. Just replace MY_PLUGIN_DIR and MY_PLUGIN_URL

# if(!defined('MY_PLUGIN_DIR'))
# {
#     define('MY_PLUGIN_DIR', str_replace('/', DS, WP_PLUGIN_DIR.DS.array_pop(explode(DS, str_replace('/', DS, realpath(dirname(__FILE__).'/..'))))));
#     define('MY_PLUGIN_URL', WP_PLUGIN_URL.'/'.array_pop(explode('/', str_replace(DS, '/', realpath(dirname(__FILE__).'/..')))));
# }

/* End of file globals.inc.php */